import React from 'react';
import MainPage from './components/MainPage'; // Import the MainPage component

const App = () => {
  return (
    <div>
      <MainPage />
    </div>
  );
};

export default App;
